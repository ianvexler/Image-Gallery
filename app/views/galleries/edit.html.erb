<h4 class="text-3xl font-normal leading-normal mt-0 mb-2 text-zinc-800">
  Edit gallery: <%= @gallery.title %>
</h4>

<% if session[:user_id] == @gallery.user_id %>
  <%= render "form", gallery: @gallery %>
<% else %>
  <h5 class="text-2xl font-normal leading-normal mt-0 mb-2 text-stone-800"> You cannot edit this gallery </h5>
<% end %>

<br>

<div style="padding-top:10px;">
  <%= link_to "← Back to gallery", @gallery, class: "hover:text-purple-900 underline" %>
</div>
