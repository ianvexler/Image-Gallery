<nav class="relative flex flex-wrap items-center justify-between px-2 py-3 bg-indigo-900 mb-3 z-20">
  <div class="container px-4 mx-auto flex flex-wrap items-center justify-between">
    <div class="w-full relative flex justify-between lg:w-auto  px-4 lg:static lg:block lg:justify-start">
      <a class="text-lg font-bold leading-relaxed inline-block mr-4 py-2 whitespace-nowrap uppercase text-white">
        IMAGE GALLERY
      </a>
      <button class="text-white cursor-pointer text-xl leading-none px-3 py-1 border border-solid border-transparent rounded bg-transparent block lg:hidden outline-none focus:outline-none" type="button" onclick="toggleNavbar('example-collapse-navbar')">
             <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
      </button>
    </div>
    <div class="lg:flex flex-grow items-center hidden" id="example-collapse-navbar">
      <ul class="table flex flex-col lg:flex-row list-none lg:ml-auto">
        <li class="inline-block text-center	nav-item">
            <%= link_to "All Galleries", galleries_path(view_name: "all_galleries"), class: "block mt-4 lg:inline-block lg:mt-0 text-white hover:text-stone-300 mr-4" %>
        </li>
        <li class="inline-block text-center	nav-item">
          <% if session[:logged_in] %>
            <%= link_to "My Galleries", galleries_path(view_name: "my_galleries"), class: "block mt-4 lg:inline-block lg:mt-0 text-white hover:text-stone-300 mr-4" %>
          <% end %>
        </li>

        <% if session[:logged_in] %>
          <li class="inline-block text-center	nav-item">
            <%= link_to "Profile", user_path(session[:user_id]), class: "block mt-4 lg:inline-block lg:mt-0 text-white hover:text-stone-300 mr-4" %>
          </li>
        <% end %>

        <li class="inline-block	text-center	nav-item">
          <%= render 'search/form' %>
        </li>
  
        <% if session[:logged_in].nil? || !session[:logged_in] %>
            <li class="inline-block text-center	nav-item">
              <%= link_to "Login", login_path, class:"ml-10 text-white bg-teal-600 hover:bg-teal-700 focus:ring-4 focus:ring-teal-200 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 focus:outline-none" %>
            </li>

            <li class="inline-block text-center	nav-item">
              <%= link_to "Sign Up", sign_up_path, class:"text-white bg-cyan-600 hover:bg-cyan-700 focus:ring-4 focus:ring-cyan-200 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 focus:outline-none" %>
            </li>
        <% end %>

        <% if session[:logged_in] %>
            <li class="inline-block text-center	nav-item">
              <%= link_to "Logout", logout_path, class:"ml-10 text-white bg-teal-600 hover:bg-teal-700 focus:ring-4 focus:ring-teal-200 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 focus:outline-none", data: { turbo_method: "post" } %>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<script>
  function toggleNavbar(collapseID){
    document.getElementById(collapseID).classList.toggle("hidden");
    document.getElementById(collapseID).classList.toggle("flex");
  }
</script>